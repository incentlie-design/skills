独立演练绑定：基础 commit `8271786fd12cc4a35339d56fb26d8635af50fa0c`，冻结候选 commit `af84c6003a6556c1a5bad9c53c25430e19b21556`，`artifact_revision=1`。commit 由任务提供，本轮未执行 Git 复核。实际仅用一次读取命令读取指定四个文件，未读取测试、报告或旧源。

**TC-001**

`behavior_status=pass`：已实际完成以下三部分文字交付。  
设计交接：`status=pass`、`readiness=design-ready`、`approval_state=candidate`、三份产物 `revision=1`。这是文字候选，不表示已注册、锁定或通过图像一致性检查。

三份产物共享以下来源绑定；来源正文由用户 fixture 提供，未读取这些真实文件，也未计算 digest。

| ref / revision | 归属与允许用途 |
|---|---|
| `canon.md@r4#CH-LAN`、`#CH-QIAO` | `canon_fact`：各自年龄、实体类型、职业及已定外貌 |
| `style.md@r2` | `user_decision`：架空内陆商埠、手绘哑光水粉及皮肤表达限制；不是服饰史证据 |
| `selection.md@r3` | `user_decision`：绑定实际选中版本；不传递造型内容 |
| `identities.md#lan-r2` → `asset-lan-identity@r2` | 用户选定文字定义；`allowed_scope=CH-LAN 已定身份特征`；`r20` 未选，不使用 |
| `identities.md#qiao-r1` → `asset-qiao-identity@r1` | 用户选定文字定义；`allowed_scope=CH-QIAO 已定身份特征` |

两项身份资产均无图像及注册证据；其已选文字状态不等于图像已审核。

`artifact_kind=character-visual-bible`，范围：CH-LAN、CH-QIAO。

| 人物与身份绑定 | 不变量／正典对照 | 表达提案与允许状态 |
|---|---|---|
| **CH-LAN 蓝舟**；`asset-lan-identity@r2`，依据 `selection.md@r3` | 46岁人类、渡船工；长窄脸、低直眉、人物自身右眉旧疤、瘦长身形；长期户外暴露 | 将中年与晒感表达为有限低对比线条、露出部位相对遮蔽部位的色块差异；旧疤用低对比短线或色块，不画成新伤。上述是画法提案。表情纹、湿润、姿态与朝向可变，不改年龄或脸部比例。 |
| **CH-QIAO 乔衡**；`asset-qiao-identity@r1`，依据 `selection.md@r3` | 25岁人类、室内抄写员；宽方脸、短卷发、厚实身形 | 青年面部以水粉色块和少量结构转折表达；室内职业不足以推定具体肤色。表情、姿态和衣物状态可变；不添加蓝舟的旧疤或日晒特征。 |

两人不是亲属或同脸设定。中景以长窄脸／宽方脸区分；远景以瘦长／厚实身形区分，不能只靠衣色；近景可使用蓝舟右眉旧疤。远景或背面无需显露疤痕。

未知项保留：蓝舟发型、两人的精确肤色基底、眼鼻口细节及绝对身高。不得将这些未知项写成已选身份事实。

消费者：人物资产使用者、分镜、服饰与连续性评审者。

`artifact_kind=wardrobe-variants`，范围：蓝舟工作装及交接货物正式变体、乔衡日常装。

以下服饰与变体 ID 均为 **proposed**，`revision=1`，未登记。设计依据是用户允许的架空创作，不声称对应真实国家或历史服饰。

| 人物／身份引用 | 服饰与变体 | 构造、用途和识别线索 |
|---|---|---|
| CH-LAN／`asset-lan-identity@r2` | `W-LAN@r1` + `V-LAN-WORK@r1` | 工作装：浅色薄内衫、灰蓝中厚短外衣、直筒长裤；肩肘留活动量，前襟短系带收束、腕口可收紧，无垂落长带。粗织布以哑光块面表现磨损。远景看短外层与瘦长身体比例，中景看内外层边缘，近景看收口与连接点；衣服不增高或增宽身体。 |
| CH-LAN／`asset-lan-identity@r2` | `W-LAN@r1` + `V-LAN-HANDOFF@r1` | 交接货物时可选：保留内衫和长裤，外层换成深灰蓝、略挺括的及髋无袖外罩，前部固定、侧面短开口；窄织边提示正式程度，不添加贵族徽记。保持肩臂活动范围，方便搬运。是否及何时换装由场景编排者选择；选择此变体本身不产生“衣物已清洗或烘干”的事实。 |
| CH-QIAO／`asset-qiao-identity@r1` | `W-QIAO@r1` + `V-QIAO-DAILY@r1` | 日常装：暖灰轻内衫、低饱和棕色齐髋外衣、宽松长裤。外衣肩背适度宽松但不改厚实身形；袖口设可拆短护袖，固定点收拢，便于伏案书写。远景看整体体量，中景看卷发与衣层，近景看护袖连接；不预设墨污。 |

**雨后连续性规则：**

`淋雨` → 当时实际穿着且被雨淋到的衣层进入湿态，色块变深、重量和贴附感增加 → `进入屋内但未烘干`，湿态继续保留 → 只有明确烘干、足以支持干燥的经过，或换上明确干燥的衣物，才能解除相应穿着湿态。

进入屋内不自动复原；未获证据不添加泥污、破损，也不断言全部内层已湿。更衣后脱下的旧衣仍保持原湿态，直到有解除事件。雨淋时具体使用工作还是交接变体未指定，交由场景编排者绑定。

服饰引用只传递上述轮廓、层次、连接、材质及附件；不传递另一张脸、身体比例、姿势、凝视或背景。消费者：场景编排者、分镜与连续性评审者。

`artifact_kind=identity-prompt`，范围：两个人物的供应商中立组合规则。

**蓝舟：**

- `identity_binding`：`CH-LAN / asset-lan-identity@r2`；正典 `canon.md@r4#CH-LAN`；选择依据 `selection.md@r3`。
- `invariants`：46岁人类；长窄脸、低直眉、右眉已愈合旧疤、瘦长身形；保留长期户外暴露依据。未定义五官细节保持未知。
- `style_binding`：`style.md@r2`；手绘哑光水粉，用色块与有限线条表达年龄、晒感和旧疤，不加入照片级毛孔。
- `wardrobe_binding`：`CH-LAN / W-LAN@r1 / V-LAN-WORK@r1 或 V-LAN-HANDOFF@r1`，均为 proposed；具体用装由场景编排者选择，不能同时默认两套。
- `variable_state`：允许表情、视向、姿態与朝向变化；已知淋雨且未烘干时延续实际穿着衣物的湿态。
- `reference_policy`：`identities.md#lan-r2` 只供已定身份维度；风格与服饰引用各限其用途。当前没有媒体参考，不能迁移参考图姿势、构图或背景。
- `forbidden_drift`：不得改用未选 `r20`，不得变年轻、变宽方脸、移除或移动旧疤、以换衣创建新人物，或切镜后自动干衣。

下一镜的身份交接仍写 `CH-LAN → asset-lan-identity@r2`。用户给出的背向、撑船交给编排者作为可变朝向和动作；不要求转头露疤或改成正脸，也不在此指定机位、景别或构图。

**乔衡：**

- `identity_binding`：`CH-QIAO / asset-qiao-identity@r1`；正典 `canon.md@r4#CH-QIAO`；选择依据 `selection.md@r3`。
- `invariants`：25岁人类；宽方脸、短卷发、厚实身形；不从室内职业推定特定肤色。
- `style_binding`：`style.md@r2`；手绘哑光水粉，青年面部以色块和少量结构线表达，不强加照片级皮肤。
- `wardrobe_binding`：`CH-QIAO / W-QIAO@r1 / V-QIAO-DAILY@r1`，均为 proposed。
- `variable_state`：表情、姿态、朝向可变；衣物湿污须由事件触发，不能继承蓝舟的淋雨状态。
- `reference_policy`：`identities.md#qiao-r1` 仅用于乔衡的已定身份；服饰引用只用于服饰。当前仅有文字定义。
- `forbidden_drift`：不得改成长窄脸或瘦长身形，不得继承蓝舟旧疤、年龄或身份版本，不因换装更换人物。

共同 handoff：将三份 `revision=1` 文字候选交给用户选定及下游编排者；待选场景服装、未定外貌细节均保留为开放项，不擅自登记。没有文件路径，因为本轮仅在回复中交付。

未执行：`media_generation=not_run`、图像身份一致性检查 `not_run`、Registry 写入 `not_run`、文件写入 `not_run`、外部研究 `not_run`。

**TC-002**

`behavior_status=pass`：实际执行了缺失输入阻断，没有猜测图片内容。  
请求结果：`status=blocked`。

实际答复：

> 目前不能给出已锁定、可供生产使用的 identity-prompt。两个文件名没有提供可读外貌内容；编号最大不能证明它是当前权威版本。即使你明确选择 `portrait_v020.png`，未见内容也不能据此描述或锁定其脸。
>
> 可以保留“古代贵族”的创作方向，但年龄、具体地域时代或明确架空决定、项目风格和人物正典仍缺失；未提供的旧项目 v020 设置不能作为通用默认。
>
> `needs_input`：请在回复中贴出人物正典与年龄、服饰的地域时代或架空决定、项目风格，以及明确选中的身份版本及其可读文字定义或参考内容。无需访问文件。
>
> 在这些输入补齐前，只能记录未知字段，不能包装成生产权威。

未执行：生产 identity-prompt `not_run`；锁定、图片读取、媒体生成、文件写入、外部服务均 `not_run`。

**TC-003**

`behavior_status=pass`：实际识别为纯执行请求，没有重跑设计或越权操作。  
请求结果：`status=blocked`。

实际答复：

> 本轮不注册，也不生成三张人物图。已批准的人物视觉、服饰定义和 prompt 保持不变。
>
> Character Registry 写入应交给应用侧获授权执行者，目前缺少写入授权；生成应交给媒体执行者，而本轮明确禁止联网、媒体模型和付费接口。人物视觉设计入口不承担这两类执行。
>
> 后续最小交接是已批准的人物 ID、身份及服饰资产的精确 revision 和批准依据。注册与生成分别取得所需授权和能力后，由对应执行者处理。本轮到此停止，不创建文件。

未执行：Registry 写入 `not_run`；三张媒体生成 `not_run`；设计修改、prompt 修改、文件写入均 `not_run`。没有声明注册或成片成功。

**有限独立意见**

```json
{
  "reviewer": "/root/c04_independent_exercise — DRAMA-SKILL-C04-REVIEW",
  "reviewed_revision": 1,
  "scope": [
    "冻结 SKILL.md、references/design-method.md 及两份公共契约",
    "三条给定原始请求的一次实际文字演练",
    "精确身份版本、身份与服饰分层、年龄及水粉表达、事件连续性、背面身份交接、缺失输入及执行权限边界"
  ],
  "status": "pass",
  "findings": [],
  "non_blocking_suggestions": []
}
```

判定依据：TC-001 能按 Skill 实际形成三类互相一致的文字候选，保留选中的低版本，并处理雨后残留和背面引用；TC-002 的缺失输入路径可直接使用；TC-003 的停止边界清楚，不要求为纯注册和生成请求重做设计。本次未发现必须修订 Skill 才能完成这三条文字行为的问题。

此结论仅表示上述文字方法的有限独立意见。未验证媒体质量、视觉同一性、应用加载、注册接口、供应商行为或集成结果，不构成集成批准。
