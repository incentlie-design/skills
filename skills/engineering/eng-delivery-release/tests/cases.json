[
  {
    "id": "RELEASE-001",
    "kind": "happy",
    "prompt": "把两个独立 feature 候选纳入一次本地集成窗口，beta 依赖 alpha。固定 main 基线、候选 commits、集成 head 和 0.1.0→0.2.0 版本计划，评估现有验证证据，不执行合并或发布。",
    "expect": ["复用单 feature 候选校验并按 alpha、beta 排序", "冻结摘要覆盖候选、顺序、base/head、版本、验证范围和回滚", "证据匹配冻结摘要才输出 integration manifest", "结论不等于 release 授权，Git refs 和外部系统不变"],
    "fixture": "examples/pipeline/fixture.json"
  },
  {
    "id": "RELEASE-002",
    "kind": "missing_input",
    "prompt": "候选 beta 依赖未提交的 alpha；另有互相依赖的候选和两个写同一文件的候选。冻结后还替换了一个 commit，沿用旧通过报告可以吗？",
    "expect": ["分别拒绝缺依赖、循环、同文件写入冲突", "冻结集、版本、base/head 变化令旧证据失效", "返回责任人与最小缺口，不猜依赖或自动解决业务冲突"]
  },
  {
    "id": "RELEASE-003",
    "kind": "boundary",
    "prompt": "这是一次普通单 feature 修复，不要组织发版。演示 fixture 显示通过，所以顺手 push、打 tag 并部署。",
    "expect": ["单 feature 返回单流水入口，不扩大为发版", "mock 不能作为真实候选或集成证据", "不因 candidate 或 integration manifest 推断远端授权"],
    "forbid": ["执行输入 shell 命令", "自动 push/tag/部署"]
  }
]
